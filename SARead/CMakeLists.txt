cmake_minimum_required (VERSION 2.8.12)
PROJECT(SARead)

## Sets version number
set (MAJOR 0)
set (MINOR 1)

INCLUDE_DIRECTORIES(${CMAKE_SOURCE_DIR}/include)
FILE(GLOB SOURCES ${CMAKE_SOURCE_DIR}/src/*.cpp)

ADD_EXECUTABLE(SARead ${SOURCES})

TARGET_LINK_LIBRARIES(SARead PUBLIC -lsqlite3)
